name: demo environment variable
on: workflow_dispatch
env:
  APP_ENV: "dev" 
jobs:
  env-var:
    runs-on: ubuntu-latest
    steps:
      - name: testing env-var
      run:
        echo "Testing.. current output from $APP_ENV"
  
